# Allow anonymous clients (no username/password)
allow_anonymous true

# Default MQTT port
listener 1883

# Persist retained messages & QoS‐queued messages
persistence true
persistence_location /mosquitto/data/

# Log to stdout so you can see it in `docker logs`
log_dest stdout
